# 2026-02-26 업데이트 내역

## 1) 변경 배경
학습 중 아래 오류가 발생함:

- `ValueError: matrix contains invalid numeric entries`
- 발생 위치: `scipy.optimize.linear_sum_assignment(...)` 호출 시점

원인은 Hungarian matching 비용행렬에 `NaN/Inf` 값이 유입되는 경우이며,
기존 코드는 `max_cost = C_g.max()` 자체가 `NaN`이 될 수 있어 치환이 실패할 수 있었음.

---

## 2) 변경 파일

1. `src/rfdetr/models/matcher.py`
2. `src/rfdetr/util/box_ops.py`

---

## 3) 파일별 변경 상세

### A. `src/rfdetr/models/matcher.py`

#### A-1. 입력 수치 안정화 추가
- 변경 위치:
  - `tgt_bbox` 정리: `85~87`
  - `flat_pred_logits`, `out_bbox` 정리: `104~107`
  - `pred_masks_logits` 정리: `135`
- 변경 내용:
  - `torch.nan_to_num(...)` 적용으로 `NaN/Inf`를 안전한 값으로 변환

#### A-2. 비용행렬(`C_g`) invalid 치환 로직 개선
- 변경 위치: `173~181`
- 변경 내용:
  - 기존: `max_cost = C_g.max(); C_g[isnan|isinf] = max_cost * 2`
  - 개선: `finite_mask = torch.isfinite(C_g)` 기반으로
    - 유한값이 있으면 `finite_max + 1.0`으로 치환
    - 전부 비유한값이면 `1e6`으로 치환
  - `torch.nan_to_num(C_g, ...)`로 일괄 정리

#### A-3. matcher chunk 옵션 전달 정리
- 변경 위치:
  - `__init__` 시그니처: `39~41`
  - 멤버 저장: `57`
  - `build_matcher` 전달: `198~217`
- 변경 내용:
  - `matcher_chunk_size`를 안전하게 받아 내부에서 사용

---

### B. `src/rfdetr/util/box_ops.py`

#### B-1. IoU 분모 0 방지
- 변경 위치: `62~64`
- 변경 내용:
  - `iou = inter / union.clamp(min=eps)`

#### B-2. GIoU 분모 0 방지
- 변경 위치: `86~87`
- 변경 내용:
  - `return iou - (area - union) / area.clamp(min=eps)`

---

## 4) 기대 효과

1. `linear_sum_assignment` 입력 비용행렬의 `NaN/Inf` 유입 차단
2. degenerate box(0 area 등) 상황에서도 IoU/GIoU 계산이 `NaN/Inf`로 깨질 확률 감소
3. 학습 중 수치적 이상치 발생 시 즉시 중단되기보다 안전한 페널티 값으로 매칭 지속

---

## 5) 검증

실행 환경: `/home/mbd1234/.new_rfdetr/bin/python`

1. 문법 체크
- `python -m py_compile src/rfdetr/models/matcher.py src/rfdetr/util/box_ops.py`
- 결과: 통과

2. degenerate box 수치 안정성 체크
- `generalized_box_iou(...)` 결과의 `torch.isfinite(...).all()` 확인
- 결과: `True`

---

## 6) 참고
이번 수정은 학습 안정화 목적이며, 추론 파이프라인(`inference.py`) 로직은 변경하지 않음.
